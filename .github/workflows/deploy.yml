name: Build and deploy

on:
  push:
    branches: [main]

# Cloudflare Pages auto-deploys on push.
# This workflow is a placeholder for future scheduled pipeline jobs.
jobs:
  ok:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Pushed to main — Cloudflare Pages will build and deploy."
